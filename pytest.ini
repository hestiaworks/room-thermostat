[pytest]
# pytest-homeassistant-custom-component's fixtures are async; without auto mode
# they resolve as generators rather than as a HomeAssistant instance.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
